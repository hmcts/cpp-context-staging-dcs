<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>uk.gov.moj.cpp.common</groupId>
        <artifactId>service-parent-pom</artifactId>
        <version>17.101.1</version>
    </parent>

    <groupId>uk.gov.moj.cpp.staging.dcs</groupId>
    <artifactId>stagingdcs-parent</artifactId>
    <version>17.101.1</version>
    <properties>
        <mvn.site.url>file://${project.build.directory}/site</mvn.site.url>
        <commons-dbutils.version>1.6</commons-dbutils.version>
        <jgitflow.maven.developBranchName>main</jgitflow.maven.developBranchName>
        <coredomain.version>17.102.11</coredomain.version>
        <progression.version>17.0.189</progression.version>
        <referencedata.version>17.0.113</referencedata.version>
        <defence.version>17.0.82</defence.version>
        <usersgroups.version>17.0.39</usersgroups.version>
        <material.version>17.0.70</material.version>
        <api-dcs-crime-version>0.9.3</api-dcs-crime-version>
        <api-cp-crime.version>0.3.2</api-cp-crime.version>
        <azure.storage.version>8.6.6</azure.storage.version>
        <azure.core.version>1.53.0</azure.core.version>
        <azure.identity.version>1.14.0</azure.identity.version>
        <netty.common.version>4.1.112.Final</netty.common.version>
    </properties>

    <packaging>pom</packaging>

    <modules>
        <module>stagingdcs-command-api</module>
        <module>stagingdcs-domain-common</module>
        <module>stagingdcs-event-processor</module>
        <module>stagingdcs-event-sources</module>
        <module>stagingdcs-healthchecks</module>
        <module>stagingdcs-integration-test</module>
        <module>stagingdcs-persistence</module>
        <module>stagingdcs-query-api</module>
        <module>stagingdcs-service</module>
    </modules>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <archiveClasses>true</archiveClasses>
                    <attachClasses>true</attachClasses>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>analyze</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        <dependency>
            <groupId>uk.gov.moj.cpp.core.domain</groupId>
            <artifactId>common-core-domain</artifactId>
            <version>${coredomain.version}</version>
        </dependency>
        <dependency>
            <groupId>uk.gov.moj.cpp.core.domain</groupId>
            <artifactId>criminal-court-public-model</artifactId>
            <version>${coredomain.version}</version>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>commons-dbutils</groupId>
                <artifactId>commons-dbutils</artifactId>
                <version>${commons-dbutils.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-common</artifactId>
                <version>${netty.common.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <site>
            <id>site</id>
            <url>${mvn.site.url}</url>
        </site>
    </distributionManagement>
</project>
